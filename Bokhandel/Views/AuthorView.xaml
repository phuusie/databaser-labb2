<UserControl
    x:Class="Bokhandel.Views.AuthorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Bokhandel.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="75"></Setter>
            <Setter Property="Height" Value="40"></Setter>
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="3"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="4*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <ComboBox Name="AuthorComboBox" Margin="5" SelectionChanged="AuthorComboBox_OnSelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run Text="{Binding Förnamn}" />
                            <Run Text="," />
                            <Run Text="{Binding Efternamn}" />
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="aUTO"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Label>Förnamn</Label>
                <TextBox Grid.Column="1" Name="NameTextBox"></TextBox>
                <Label Grid.Row="1">Efternamn</Label>
                <TextBox Grid.Row="1" Grid.Column="1" Name="LastnameTextBox"></TextBox>
                <Label Grid.Row="2">Födelsedatum</Label>
                <DatePicker Grid.Row="2" Grid.Column="1" Margin="3" Height="40" Name="BirthdateDatePicker"></DatePicker>
            </Grid>
            <StackPanel Grid.Row="3" HorizontalAlignment="Center" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Name="AddBtn" Click="AddBtn_OnClick">Lägg till</Button>
                <Button Name="UpdateBtn" Click="UpdateBtn_OnClick">Uppdatera</Button>
                <Button Name="ClearBtn" Click="ClearBtn_OnClick">Rensa</Button>
                <Button Name="RemoveBtn" Click="RemoveBtn_OnClick">Ta bort</Button>
            </StackPanel>
        </Grid>
        <ListView
            Grid.Row="1"
            Grid.Column="2"
            Margin="5"
            Background="#e0e8eb"
            Name="AuthorsBooksListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Isbn13}" Header="ISBN" />
                    <GridViewColumn DisplayMemberBinding="{Binding Titel}" Header="Titel" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
